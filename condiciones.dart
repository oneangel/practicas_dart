void main() {
  //Tipos bool y condiciones
  
  bool? isActive = null;
  
  if (isActive==null)
    print("El valor de la variable es null");
  else if (isActive)
    print("El valor de la variable es true");
  else
    print("El valor de la variable es false");
  
  /*
  //if simple
  if (isActive){
    print("El contenido de la variable es verdadero");
  }
  
  //if compuesto
  if (isActive){
     print("El contenido de la variable es verdadero");
   } else {
      print("El contenido de la variable es falso");
    }
    
  */
  
  
  
  
}

